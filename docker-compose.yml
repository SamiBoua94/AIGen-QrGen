version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - DATABASE_URL=file:./database.db
      - NEXT_PUBLIC_BASE_URL=http://localhost:3000
    restart: always
